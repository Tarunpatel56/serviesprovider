import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color.fromARGB(255, 60, 133, 241);   // Main color
  static const Color secondary = Color(0xFFFFA500); // Optional
  static const Color background = Color(0xFFF5F5F5);
    static const Color background2 = Color.fromARGB(255, 213, 211, 206);
      static Color headerBg = Colors.blue.shade600;
  
}